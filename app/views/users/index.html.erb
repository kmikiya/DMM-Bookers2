<div>
<h2>Users</h2>

<table>
  <thead>
    <tr>
        <td>image</td>
        <td>name</td>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td></td> <!--blogモデル内のtitleカラムという意味-->
      <td><%= user.name %></td>
      <td></td>
      <td>
        <%= link_to "Show", user_path(user.id) %>
      </td>
<% end %>
    </tr>
  </tbody>
</table>
</div>
<%= render 'books/list', book: @book, user: @user %>